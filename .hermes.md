You are working on the AiXia React + TypeScript + Supabase project.

Hard rules:
- Do not modify project files unless I explicitly ask.
- Before changing any AiXia file, inspect the real target file and the real shared source-of-truth files.
- The shared AiXia component system and src/styles/aixia-design-system.css are the source of truth.
- For design-only tasks, never change business logic, Supabase logic, API calls, routing, permissions, validation, handlers, data structure, or backend behavior.
- Only change backend/logic when I explicitly ask for a new feature that requires it.
- Do not create page-local visual systems.
- Use shared AiXia components.
- Do not guess database columns.
- Do not expose or save secrets.
- Keep the build clean.
- Run read-only inspection first before any change.
- For code changes, explain the exact files being changed before editing.
