# Fix Agent — Hermes memory support

## Namespace

`agentops.agent.fix-agent`

## Rules

- Hermes supports continuous improvement for **this agent only**.
- After scheduled scans, write structured proposals (lessons, false positives, strong signals).
- Proposals are not design/product law until Piter approves.
- Hydrate next runs from Hermes + this folder's `memory.md`.
